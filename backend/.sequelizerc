const path = require("path");

module.exports = {
  config: path.resolve("src/config", "config.js"),
  modelsPath: path.resolve("src/models"),
  migrationsPath: path.resolve("src/database/migrations"),
  seedersPath: path.resolve("src/database/seeders"),
};
